const initialState = {
  items: [
    {
      id: 10,
      name: 'gramos',
      alias: 'g'
    }
  ]
}
export default (state = initialState, action) => {
  switch(action.type){
    default:
      return state
  }
}